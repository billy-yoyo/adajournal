
export interface ScaleQuestionComponentProps {
    id: number;
    creating: { error?: any };
}

export default function ScaleQuestionEditor({} : ScaleQuestionComponentProps) {
    return <div></div>
}
